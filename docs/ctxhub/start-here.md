# Golden Hour web — ctxhub entry

This repo (`goldenhour-web`) shares the **@skawful/golden-hour** drive with the mobile app.

**Canonical team docs** are maintained in the mobile repo and pushed to the drive from `speak-easy/docs/ctxhub/`. Start with the drive file **`start-here.md`** (project index).

## Web-specific doc on this drive

After push, see **`web-app.md`** for routes, env vars, and module map.

## This repo

- GitHub: [ritch/goldenhour-web](https://github.com/ritch/goldenhour-web)
- Run: `npm run dev` with `.env.local` (see `.env.example`)
- Backend/migrations: always in `speak-easy/supabase/`
